{
  "name": "invoke-chain",
  "version": "1.0.10",
  "description": "Invoke functions as chain",
  "private": false,
  "main": "src/main.ts",
  "keywords": ["chain", "function chain", "invoke chain", "invoke function chain", "ts"],
  "repository": {
    "type": "git",
    "url": "https://github.com/karpov-dev/InvokeChain-JS"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build"
  },
  "author": "Uladzimir Karpau",
  "license": "ISC",
  "devDependencies": {
    "nanoid": "^3.3.4",
    "typescript": "^4.6.4"
  }
}
